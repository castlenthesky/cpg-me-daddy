# @cpg/graph-builder

Status: **stub** — `src/index.ts` exports only `hello()`.

Intended: assemble/update the persistent graph model from `@cpg/cpg-generator` output, for `@cpg/graph-visualizer` to render.
