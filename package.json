{
  "name": "cpg-me-daddy",
  "version": "0.0.1",
  "private": true,
  "description": "Code Property Graph for TypeScript/JavaScript/Python — workspace root",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "oxlint",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "typecheck": "tsc --build",
    "watch": "tsc --build --watch",
    "typecheck:clean": "tsc --build --clean",
    "test:unit": "bun test packages/engine/test/unit packages/cli/test/unit packages/vscode/test/unit",
    "prepare": "lefthook install || true"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "@types/node": "22.x",
    "lefthook": "2.1.12",
    "oxfmt": "0.67.0",
    "oxlint": "1.82.0",
    "typescript": "^5.9.3"
  }
}
