# cpg-me-daddy

Bare-bones VS Code extension scaffold. See `docs/plans/unified-architecture.md` for the target architecture.

## Running

```
npm install
npm run compile
```

Then press F5 in VS Code to launch the Extension Development Host.
