{
  "//": "Solution file. Builds every workspace package: `tsc --build` from the repo root.",
  "files": [],
  "references": [
    { "path": "packages/engine" },
    { "path": "packages/cli" },
    { "path": "packages/vscode" }
  ]
}
